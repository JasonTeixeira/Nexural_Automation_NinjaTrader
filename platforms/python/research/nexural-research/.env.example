# Copy to `.env` for local overrides.

# Optional: default folder where you drop NinjaTrader exports
NEXURAL_EXPORTS_DIR=./data/exports

# Optional: Discord webhook for future notifications
DISCORD_WEBHOOK_URL=
